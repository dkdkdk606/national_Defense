[Day1 학습내용]

1. 과정소개(교육 로드맵, 교과목내용, 일정, 기타)

2. 파이썬 소개 : 언어적 특징, 장단점

3. 파이썬 환경구축 : python3, VS Code 설치
    python3 : IDLE, 명령프롬프트(Ipython)
	인터랙티브 셀(Ipython) : >>>
	스크립트(편집) 모드 : 편집기  
		.py 파일저장 -> 실행(F5)
   VS Code : 
	확장기능 설치 : Python(MS)
	폴더 설정 : 
	인터프리터 설정 : 3.14.6 (python.exe) (Ctrl+Shift+p)
	코드 작성과 실행 : 
	부분 실행 : <shift>+<enter>
	단축키 설정/변경/제거

작업공간 :  C:\Workspaces\01_python\01_Basic

4. 파이썬 기초
   1) 표현식, 문장, 프로그램
   2) 식별자 : 사용자정의한 함수, 변수, 클래스 이름
        규칙 : 문자+숫자, 특수문자 중 _ 가능
        snake type / camel type
   3) 키워드(keyword) : 예약어 => 35개
       문법적인 의미를 갖는 단어
       -> 식별자로 사용 불가
   4) 주석문(comment) : # -> 한줄 주석
        여러 줄 주석 : 
	'''    주석내용1
	주석내용2

	  '''
	"""    주석내용1
	주석내용2

	  """
  5) print( , , , ....) : 출력함수


5. 변수
1) 변수(variable)
   데이터(값, 객체)를 갖는 메모리 공간
   변수에 값이 저장된 주소를 갖음
   변수이름 = 값(수식, 함수)
  파이썬 변수는 데이터유형이 변경 가능(동적타이핑)
   type()
   id()

  a = 10
  b = a
  c = e = f = 100
  x, y, z = 1, 3.1 , True
  a = a + 10

2) print() 함수를 이용한 변수의 값 출력
  print(, , ,)
  print(숫자 + 숫자)
  print(문자열 + 문자열)
  print(문자열 + str(정수))
  print( , , , sep='/')

3) 문자열 포맷팅(formatting) => 포맷서식
① %포맷
  '     %s ' % 변수  : 문자열
  '     %d ' % 변수  : 정수
  '     %f ' % 변수  : 실수
  '     %c ' % 변수  : 문자

② format() 함수
 format(변수, 서식)
 format(12300, ',')  => '12,300'

③  '  {0:f} {1:s} '.format( , )

④ f-string : f'{name:s} {age:5d} '





